{%if settings.dgHomeLink === true%}
    <nav class="navbar">
        <div class="navbar-inner">
            <a href="/" class="navbar-logo-link" style="text-decoration: none;">
                <img src="/img/logo.png" alt="{{meta.siteName}}" class="navbar-logo" />
                <h1 style="margin: 15px !important;">{{meta.siteName}}</h1>
            </a>
        </div>
        {% if settings.dgEnableSearch === true%}
            {% include "components/searchButton.njk" %}
        {%endif%}
    </nav>
    {%else%}
    <div class="empty-navbar" >
        {% if settings.dgEnableSearch === true%}
            {% include "components/searchButton.njk" %}
        {%endif%}
    </div>
{%endif%}